
import Home from './(app)/page';

// Exporta el componente del dashboard para que sea la página de inicio.
// Al importar desde la ruta '(app)', nos aseguramos de que se renderice dentro del layout principal.
export default Home;
